"""
todo function download the most recent gfs data, updates the controllers context, set the wms bounds, resample and do averaging
     get called by ajax probably and return a success so that you can make the page refresh when you finish
"""